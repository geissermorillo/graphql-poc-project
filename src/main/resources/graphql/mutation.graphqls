scalar NonNegativeInt
scalar DateTime
scalar Date

# All mutations available in graphqls
type Mutation {
    # Creates a Bank Account
    createBankAccount(input: CreateBankAccountInput!): BankAccount!

    # Upload a file
    uploadFile: ID!
}